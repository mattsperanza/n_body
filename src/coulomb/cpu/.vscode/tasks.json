{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Release",
            "type": "shell",
            "command": "${workspaceFolder}/Compile.sh -j 3"
        },
        {
            "label": "Build Debug",
            "type": "shell",
            "command": "${workspaceFolder}/CompileDebug.sh -j 3"
        }
    ]
}